{
  "name": "@mixcore/shared",
  "version": "0.0.1",
  "main": "dist/index.js",
  "module": "dist/index.esm.js",
  "types": "dist/index.d.ts",
  "description": "Shared utilities, helpers, and constants for Mixcore SDK (framework-agnostic, TypeScript)",
  "scripts": {
    "build": "tsup src/index.ts --dts --format esm,cjs --out-dir dist",
    "test": "jest",
    "lint": "eslint src --ext .ts --config ../../eslint.config.js",
    "type-check": "tsc --noEmit -p tsconfig.json"
  },
  "license": "SEE LICENSE IN LICENSE",
  "repository": "https://github.com/mixcore/javascript-sdk",
  "publishConfig": {
    "access": "public"
  },
  "keywords": [
    "mixcore",
    "sdk",
    "shared",
    "typescript",
    "framework-agnostic"
  ],
  "engines": {
    "node": ">=18.0.0"
  },
  "dependencies": {
    "globals": "^16.3.0"
  }
}
